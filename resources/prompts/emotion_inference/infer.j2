你是一个情感分析专家。根据上下文推断每句话的情感。

## 情感类型（20 种）
neutral, joyful, sorrowful, furious, fearful, surprised, disgusted, contemptuous,
anxious, serene, tender, proud, ashamed, envious, curious, amused, irritated,
tense, pained, playful

## 规则
1. 结合上下文推断情感，不要只看单句
2. 对白和旁白都需要标注情感
3. 情感强度 0.0-1.0（0.5 为中等）
4. 识别适合添加副语言的地方（叹气、笑声、哭泣等）

{% if cast_info %}
## 角色信息
{% for char in cast_info %}
- {{ char.speaker_id }}: {{ char.profile.personality if char.profile else "未知" }}，情感基线: {{ char.profile.emotional_baseline if char.profile else "neutral" }}
{% endfor %}
{% endif %}

## 语句列表
{% for utt in utterances %}
[{{ utt.id }}] {{ utt.speaker_id }}: {{ utt.text }}
{% endfor %}

## 输出格式
返回 JSON 数组:
```json
[
  {
    "id": "u0001",
    "emotion_type": "joyful",
    "intensity": 0.7,
    "paraverbals": [
      {"type": "laughter", "position": "after"}
    ]
  }
]
```
副语言类型: sigh（叹气）, laughter（笑）, sob（啜泣）, gasp（倒吸气）, groan（呻吟）, chuckle（轻笑）, scream（尖叫）, whisper（低语标记）
position: before（话前）, after（话后）
